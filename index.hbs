{{!< default}}
{{!-- Fallback channel template. The apex `/` is overridden by routes.yaml to
     render the bs-home page via custom-bs; this is only reached for ad-hoc
     channel URLs (e.g. /page/2/). Kept minimal — just routes the visitor home. --}}

<section class="section">
  <div class="reveal">
    <div class="eyebrow">AMPM Building Services</div>
    <h1 class="display">Buildings done <em>properly.</em></h1>
    <p class="deck" style="margin-top: 20px; max-width: 60ch;">
      M&amp;E, fit-out, fabric, roofing and groundworks under one roof, one PM, one Form of Tender.
    </p>
    <p style="margin-top: 32px;">
      <a class="btn primary" href="/">Start at home →</a>
    </p>
  </div>
</section>
